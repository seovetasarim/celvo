# Yerel: copy .env.example .env.local  |  Vercel: Project → Settings → Environment Variables

# Canlı site adresi (SEO, Open Graph, JSON-LD)
NEXT_PUBLIC_SITE_URL=https://celvo.com.tr

# MySQL (cPanel — Remote MySQL → Access Hosts: %)
DB_HOST=104.247.173.212
DB_USER=celvocom_ilen
DB_PASSWORD=
DB_NAME=celvocom_celvo
# MySQL portu 3306 — 2083 cPanel HTTPS’tir, veritabanı için kullanılmaz
DB_PORT=3306

# Uzak sunucuda genelde gerekir (Vercel Production + Preview)
DB_SSL=true
